{
  "name": "@hrmonic/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "HRMONIC — HR-Verwaltung für den deutschsprachigen Markt",
  "author": "HRMONIC",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --dev && electron . ",
    "typecheck": "tsc --noEmit",
    "dist:win": "electron-builder --win",
    "dist:mac": "electron-builder --mac",
    "dist:linux": "electron-builder --linux"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "electron": "^38.0.0",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
