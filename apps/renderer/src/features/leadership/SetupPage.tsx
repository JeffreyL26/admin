import React from 'react';
import { PlaceholderPage } from '../../components/PlaceholderPage';

// PLATZHALTER — wird durch die eigentliche Seite ersetzt.
export function SetupPage() {
  return <PlaceholderPage title="SetupPage" />;
}
