import type { FastifyPluginAsync } from 'fastify';

// Modul: Kommunikation & Engagement (wird in der Modul-Phase implementiert).
export const communicationModule: FastifyPluginAsync = async () => {};
