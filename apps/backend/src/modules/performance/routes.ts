import type { FastifyPluginAsync } from 'fastify';

// Modul: Leistungsverwaltung & Entwicklung (wird in der Modul-Phase implementiert).
export const performanceModule: FastifyPluginAsync = async () => {};
