import type { FastifyPluginAsync } from 'fastify';

// Modul: Personalverwaltung & Stammdaten (wird in der Modul-Phase implementiert).
export const employeesModule: FastifyPluginAsync = async () => {};
